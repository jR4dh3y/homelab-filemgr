# Homelab File Manager - Environment Configuration
# Copy this file to .env and customize for your setup

# ===========================================
# Security Settings (CHANGE THESE!)
# ===========================================

# JWT secret for authentication - use a long random string
# Generate with: openssl rand -base64 32
JWT_SECRET=change-me-in-production-use-a-long-random-string

# User credentials (username:password)
# Format: FM_USERS_<username>=<password>
# Example: FM_USERS_admin=your-secure-password
# If no users configured, defaults to admin:admin
FM_USERS_admin=admin

# ===========================================
# Port Configuration
# ===========================================

# Main application port (nginx proxy)
PORT=3000

# Backend API port (internal, usually not exposed)
BACKEND_PORT=8080

# Origin URL for CORS (change for production)
ORIGIN=http://localhost:3000

# ===========================================
# Rate Limiting
# ===========================================

# Auth endpoint rate limit (requests per second per IP)
# Set to 0 to disable rate limiting
FM_RATE_LIMIT_RPS=10

# ===========================================
# CORS / WebSocket Origins
# ===========================================

# Allowed origins for WebSocket connections and CORS
# Leave empty to allow all origins (homelab mode)
# Format: comma-separated list
# Supports wildcards: *.example.com
# Example: FM_ALLOWED_ORIGINS=http://localhost:3000,https://myapp.example.com
FM_ALLOWED_ORIGINS=

# ===========================================
# User Settings
# ===========================================

# Your home directory path (e.g., /home/john)
HOME_PATH=/home/user

# ===========================================
# Optional: User/Group ID Mapping
# ===========================================
# If you need specific UID/GID for file permissions
# Uncomment and set these values

# PUID=1000
# PGID=1000

